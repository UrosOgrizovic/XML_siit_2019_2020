<let:CoverLetters
        xmlns:let="http://localhost:8080/Letter"
        xmlns:pred="http://localhost:8080/Letter/predicate/"
        xmlns:Users="http://localhost:8080/Users"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://localhost:8080/SciencePapers"
>
  <let:CoverLetter journalName="string" journalAddress="string" manuscriptTitle="string" articleType="review"
                   submissionDate="2008-09-29">
    <let:Content>string</let:Content>
    <!--1 or more repetitions:-->
    <let:Author>
      <let:FullName>string</let:FullName>
      <let:Institution>string</let:Institution>
      <let:EMail>string</let:EMail>
    </let:Author>
  </let:CoverLetter>
</let:CoverLetters>